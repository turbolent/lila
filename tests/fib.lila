// fib

define function fib (n) {
  if <(n, 2) {
    n
  } else {
    +(fib(-(n, 1)),
      fib(-(n, 2)))
  }
};

let start = time() {
  print(as-string(fib(35)));
  let end = time() {
    -(end, start)
  }
};
